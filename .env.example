# Example: How to avoid hardcoding secrets in Spring Boot

# ❌ BAD: Hardcoded secrets (will be detected and blocked)
spring.datasource.url=jdbc:postgresql://localhost:5432/petclinic
spring.datasource.username=admin
spring.datasource.password=MySecretPassword123!

# ✅ GOOD: Use environment variables
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/petclinic}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD}

# ✅ GOOD: External configuration
# Use Spring Cloud Config Server or Kubernetes Secrets
# Don't put real secrets in application.yml/properties

# For local development, use:
# 1. .env file (add to .gitignore)
# 2. application-local.yml (add to .gitignore)
# 3. Environment variables
# 4. IDE run configurations

# For production, use:
# 1. Kubernetes Secrets
# 2. AWS Secrets Manager
# 3. Azure Key Vault
# 4. HashiCorp Vault
# 5. Spring Cloud Config with encryption
